# PortScanner config file example
{
    # PS will scan only for these ports and log the result of each connection
    "localhost": [  # test
        "80",
        "8080",
    ],

    # PS will scan port 1 to 1025 and log the open ones
    "127.0.0.1": [],
}
